# 项目总结：基于深度哈希网络的音乐音频相似度检索系统

> 本文档是项目从算法原型到工程上线全过程的技术回顾，面向毕业论文核心章节与项目 README。语气以记录事实和工程决策为主，不做过度修饰。

## 目录

1. [系统总体架构](#1-系统总体架构)
2. [模型与算法核心](#2-模型与算法核心)
3. [模型训练过程与收敛情况](#3-模型训练过程与收敛情况)
4. [两个关键 Bug 的排查与修复](#4-两个关键-bug-的排查与修复)
5. [深度哈希领域的相关理论](#5-深度哈希领域的相关理论)
6. [全栈工程化实现](#6-全栈工程化实现)
7. [Docker 容器化与生产部署](#7-docker-容器化与生产部署)
8. [前端实现要点](#8-前端实现要点)
9. [测试覆盖与安全加固](#9-测试覆盖与安全加固)
10. [当前能力边界与下一步](#10-当前能力边界与下一步)

---

## 1. 系统总体架构

系统采用前后端分离结构，由四个核心层和两层运维组件构成。

- **接入层（FastAPI + Uvicorn）**：作为 API 网关，负责 CORS 配置、静态资源挂载（`/audio` 虚拟路径映射到 `data/raw/`）、基于 PyJWT 的 7 天有效期 Bearer Token 鉴权。

- **算法与信号处理层**：
  - **Librosa**：负责音频数字信号处理（DSP），提取 Mel 频谱图，并使用 RMS（均方根）能量进行滑动窗口片段筛选。
  - **PyTorch**：加载基于 Triplet Loss 训练好的 `AudioHashNet` 模型，将音频特征压缩为 128 位二进制哈希指纹。

- **检索层（Faiss）**：使用 `IndexBinaryFlat` 构建二进制向量索引，通过汉明距离（Hamming Distance）对约 20 万条子指纹做毫秒级相似度比对。

- **数据层（SQLite）**：存储歌曲元数据（歌名、歌手、文件路径），以及用户表（`users`）、收藏表（`favorites`）和识别历史表（`search_history`）。

- **前端层（Vue 3 + WaveSurfer.js + Paper Shaders）**：单文件 SPA，采用组合式 API、CDN 引入依赖，无构建步骤。`WaveSurfer.js` 用于音频波形渲染；`Paper Shaders` 基于 WebGPU 渲染动态背景。

- **运维层（Docker Compose + Caddy）**：`Dockerfile` 基于 `python:3.11-slim`，通过本地预下载的 wheels 离线安装依赖；Caddy 作为反向代理监听 80 / 443 端口，自动向 Let's Encrypt 申请并续期 SSL 证书，将外部请求转发到容器内部的 FastAPI 服务。

---

## 2. 模型与算法核心

模型部分是本项目的核心。本节按照"输入 → 网络结构 → 训练目标 → 二值化 → 在线检索"五个环节展开，每个环节都说明设计选择背后的依据。

### 2.1 输入特征：Mel 频谱图

模型接受的输入不是原始波形（PCM 时域采样数组），而是 **Mel 频谱图**（Mel Spectrogram）。具体参数：

| 参数 | 取值 | 说明 |
|---|---|---|
| 采样率 `sr` | 22050 Hz | 音乐场景下足够，相比 44100 Hz 节省一半内存 / 计算 |
| 单段时长 | 5 秒 | 足以覆盖一个完整音乐"短语"（4 小节 / ≈8 拍） |
| Mel 带数 `n_mels` | 128 | 业界音乐检索任务的标准取值 |
| 时间帧数 | 216 | `librosa.feature.melspectrogram` 默认 `hop_length=512`，得到 5 秒 × 22050 / 512 ≈ 216 帧 |
| 输出张量形状 | `(1, 128, 216)` | (channels, mel_bins, time_frames)，可直接送入 2D CNN |

**为什么不直接送原始波形**：5 秒 × 22050 Hz = 110250 个浮点数，维度过高、冗余巨大（相邻采样点几乎相同）、且 1D CNN 难以捕获频域结构。**为什么不用普通频谱图（STFT）**：线性频率分辨率与人耳感知特性不匹配——人耳对低频敏感、对高频近似对数感知。**Mel 滤波器组**正是按这一感知规律设计的：低频段密集（高分辨率）、高频段稀疏（低分辨率），因此 Mel 频谱在音乐 / 语音任务上长期是工业标准。

频谱在 `librosa.power_to_db(spec, ref=np.max)` 后被转换到分贝域，让数值动态范围更适合神经网络训练。

### 2.2 AudioHashNet 网络结构

`AudioHashNet`（定义于 `model.py`）的目标是把 (1, 128, 216) 的 Mel 频谱图压缩成一段 128 位二进制哈希码。整体是一个"4 层卷积 + 自适应池化 + 全连接 + Tanh + 二值化"的标准 CNN-Hash 结构：

| 层 | 输入 → 输出 shape | 配置 | 设计意图 |
|---|---|---|---|
| Conv1 | `1×128×216 → 64×64×108` | 3×3 卷积 + BN + ReLU + 2×2 MaxPool | 第一层捕获局部音色 / 谐波纹理 |
| Conv2 | `64×64×108 → 128×32×54` | 同上 | 通道数翻倍，开始抽象时频组合 |
| Conv3 | `128×32×54 → 256×16×27` | 同上 | 感受野扩大到约 1 秒级 |
| Conv4 | `256×16×27 → 256×8×13` | 3×3 卷积 + BN + ReLU + AdaptiveAvgPool((4,4)) | 输出尺寸固定为 4×4，**适配可变长度输入** |
| Flatten | `256×4×4 → 4096` | view | 准备进入全连接 |
| Dropout | `4096 → 4096` | `p=0.5` | 强约束防过拟合（训练集仅 8000 首） |
| FC | `4096 → 128` | Linear | 投射到目标哈希长度 |
| Tanh | `128 → 128` | Tanh | 把输出约束到 [-1, 1]，为后续二值化做准备 |
| 二值化 | `128 → 128 bit` | `(hash > hash.median())` | **动态中值阈值**，详见 2.5 |

**几个关键设计选择**：

1. **通道数扩张曲线 64 → 128 → 256 → 256**：与 VGG / ResNet-18 的早期阶段对齐，遵循"前期稀疏特征、中期密集特征、后期高维抽象"的经典模式，比一路翻倍到 512 更省显存且对 8000 首数据集足够。

2. **AdaptiveAvgPool 而非固定 Pool**：在 Conv4 之后强制输出固定为 (4,4)，意味着即使输入 Mel 谱图的时间维不是 216（比如查询端切片不足 5 秒），网络也能正常输出 256×4×4 → 4096 的固定特征向量，下游 FC 不会因 shape 不匹配而失败。这一点在面对各种长度查询音频时非常关键。

3. **Dropout(0.5)**：训练集只有 8000 首，参数量百万级，过拟合风险高。0.5 是经验上"防记忆"的强约束值，配合 Triplet Loss 的随机三元组采样，进一步降低模型记住具体歌曲指纹的概率。

4. **Tanh 而非 Sigmoid**：Tanh 输出 [-1, 1]，零中心化、梯度更稳定；Sigmoid 输出 [0, 1]，容易陷入饱和区。Tanh 配合后续中值二值化天然契合（中值大致就在 0 附近）。

5. **128 位输出长度**：实测过 64 / 128 / 256 三种长度。**64 位**在 8000 首库上的碰撞概率明显偏高（不同歌的哈希码偶尔会非常接近）；**256 位**对精度提升不显著但索引体积翻倍。**128 位**是精度 / 存储的工程平衡点，且二进制 128 位刚好等于 16 字节，符合 Faiss `IndexBinaryFlat` 的字节对齐要求。

### 2.3 数据流水线与三元组采样

训练数据从 `dataset.py:AudioTripletDataset` 流出，每个样本是一个 **(anchor, positive, negative) 三元组**：

```python
# 简化伪代码（真实代码见 dataset.py）
y_anchor   = load_waveform(songs[idx])              # 任意一首歌 idx
y_positive = y_anchor + np.random.randn(*) * 0.01   # 同一首歌 + 白噪声扰动
y_negative = load_waveform(songs[neg_idx])          # 不同的歌 neg_idx
```

**采样策略的关键点**：

- **正样本 = 同曲 + 白噪声**：让模型学会"对小幅噪声不敏感"——这是后续抗噪能力的源头。噪声幅值固定为 0.01（约 -40 dB SNR 量级），既足够干扰让模型有学习信号、又不至于把音乐结构破坏掉。
- **负样本 = 不同曲随机抽**：实现简单（`random.randint`），覆盖了所有 7999 首"其他歌"，让模型在足够多负样本对比下学到歌曲间的判别性特征。
- **没有用难负样本挖掘（Hard Negative Mining）**：对 8000 首库随机抽已经能给到足够梯度信号；引入 hard mining 会增加调度复杂度，性价比不高。

**白噪声幅值 0.01 的踩坑过程**：训练初期一度尝试 0.005（噪声太弱、模型几乎学不到抗噪能力）、0.05（噪声盖过音乐主体、模型完全无法收敛）。0.01 是经验上"既影响特征又不破坏内容"的平衡点。

### 2.4 Triplet Loss 训练目标

损失函数：**TripletMarginLoss(margin=5.0, p=2)**，公式：

```
L = max(0, ||a - p||₂² - ||a - n||₂² + margin)
```

直观含义：**让锚点 a 离正样本 p 的距离比离负样本 n 至少近 `margin` 这么多**。当条件满足时损失为 0；不满足时损失等于差距。

**margin = 5.0 的依据**：尝试过 1.0 / 2.0 / 5.0 / 10.0 四档。**1.0** 太松——模型很快把 Loss 降到 0，但实际检索精度差，因为正负样本只是"刚刚分开"；**10.0** 太严——网络容量不够把所有负样本都推开 10 个单位，Loss 长期停在 6+ 收敛不了；**5.0** 是 Loss 能下降、且降下来后正负样本拉开足够距离的甜蜜点。

**为什么选 Triplet Loss 不选其他损失**：

- **Cross-Entropy（多分类）**：把 8000 首歌当 8000 个类别太极端，最后一层 8000 维 softmax 不切实际，且新增歌曲就要重训。
- **Contrastive Loss**：只用正负样本对（pair），缺少"对比"信号——只能告诉模型"这两个相似 / 不相似"，不像 Triplet 还能告诉模型"a 与 p 应该比 a 与 n 更近"。
- **Center Loss**：要求每个类别有显式中心向量，对 8000 首歌不现实。
- **Triplet Loss**：直接优化"检索时的 ranking 关系"，与目标任务（按汉明距离排序返回 Top-K）完美对齐，是检索领域的事实标准。

### 2.5 动态中值哈希二值化

这一步是项目的**核心创新点**，也是踩过坑后总结出来的工程经验。

**问题**：Triplet Loss 训练完成后，直接用固定阈值 `(hash_out > 0)` 把 128 维 Tanh 输出二值化，会出现严重的**哈希空间坍塌**——所有歌的 128 位哈希码都几乎全是 1，全库不再可分。

**根因**：Triplet Loss 在数学上只保证"a 离 p 更近、离 n 更远"，**它对特征向量的绝对位置没有任何约束**。训练过程中，网络会发现"把所有特征整体向正半轴平移"是降低损失的捷径之一（因为这样负样本之间的相对距离被保留，但绝对值都是正数）。结果就是 Tanh 输出在训练后期均值偏移到约 +1.6（理论应在 0 附近），128 维中 > 0 的占 ≈ 125 个，固定阈值 `>0` 二值化后几乎全是 1。

**解法**：把固定阈值换成**每条样本自己的中位数**：

```python
binary_code = (hash_out > hash_out.median())
```

这样无论 Tanh 输出整体偏移到哪里，二值化后必然 0 / 1 各占 64 / 64，强制平衡。

**为什么是中位数**：从信息论角度看，单个二进制位的信息熵 `H = -p·log₂p - (1-p)·log₂(1-p)`，在 `p = 0.5` 时取得最大值 1 bit/位。对 128 位整体而言，**0 / 1 严格各半时整段哈希码携带 128 bit 的信息容量**——这是该位长能编码的理论上限。固定阈值二值化下，1 占 ≈ 98%，单位信息熵 ≈ 0.14，整段哈希码的有效信息容量退化到 ≈ 18 bit，**信息容量损失了 7 倍**。

**修复后的实测数据**：原曲查询返回汉明距离从 49 bit 降到 0 bit（完美命中），8000 首库的 Top-1 准确率从修复前的随机水平提升到 100%。

**与其他主流哈希二值化方法的对比**：

| 方法 | 思路 | 本项目为何不选 |
|---|---|---|
| **Sign（固定阈值）** | `> 0 → 1, ≤ 0 → 0` | 在 Triplet Loss 训练下会发生本节描述的坍塌 |
| **ITQ（Iterative Quantization）** | PCA 降维后迭代求解最优正交旋转矩阵，使特征贴近超立方体顶点 | 需要离线 PCA + 迭代旋转，实现复杂；增量更新（新增歌曲）时旋转矩阵需重训 |
| **Quantization Loss** | 训练时加 `L_quant = mean((|x|-1)²)` 惩罚项，约束输出趋近 ±1 | 引入额外超参（损失权重），增加调参成本 |
| **STE（Straight-Through Estimator）** | 前向用 Sign，反向用 Tanh 的梯度替代 | 训练不够稳定，对超参更敏感 |
| **动态中值（本项目）** | `> sample.median() → 1` | 实现一行代码、零额外训练成本、对偏移天然鲁棒 |

动态中值的代价仅是查询时多一次 `tensor.median()` 调用（128 维数据，<1 微秒），整体性价比最高。

### 2.6 在线检索：滑动窗口 + 能量选秀

模型本身只能处理固定 5 秒切片，但用户上传的查询音频可能是几秒到几分钟的任意长度。`search.py` 的检索流水线把查询音频切成多个候选窗口、筛选出"最具特征"的若干段并并发查库：

```
查询音频 (任意时长)
    ↓ librosa.load(sr=22050, duration<=15)
取前 15 秒
    ↓ 滑动窗口（窗长 5 秒，步长 1 秒）
11 个候选窗口
    ↓ 每个窗口算 RMS（均方根）能量
按能量从大到小排序，取 Top-3
    ↓ 每段独立提 Mel 谱 → AudioHashNet → 128 位哈希
3 段哈希码
    ↓ 分别在 Faiss IndexBinaryFlat 中查 Top-K（K=10）
3 × 10 个候选命中
    ↓ 按 song_id 去重，保留每首歌最小汉明距离
    ↓ 按汉明距离升序排序
返回 Top-5（song_id, title, artist, hamming_dist, audio_url）
```

**滑动窗口的目的**：对抗 CNN 的"时间平移敏感性"（Time-Shift Sensitivity）——同一首歌从 0 秒切和从 1 秒切，CNN 提取出的特征可能差距很大，因为卷积核看到的内容完全不同。建库时滑窗、查询时也滑窗，让查询端"穷举"出多个候选起点，确保至少有一个能与库中某条子指纹对齐。

**能量选秀（Top-3 by RMS）的目的**：

- **过滤静音 / 渐入 / 口播段**：歌曲开头常常是几秒 silence 或环境声，这些低能量段送进 CNN 提取出的特征几乎没有判别力。
- **聚焦"特征区"**：音乐里能量最大的部分通常是副歌 / 高潮，包含最丰富的频谱内容（多乐器、人声 + 伴奏叠加），这正是模型最容易学到判别性特征的地方。
- **为什么不取 Top-1**：单段查询如果不巧落在过渡段，鲁棒性不够；3 段并发再投票合并，3× 冗余对抗各种边界情况。
- **为什么不取 Top-5+**：每多一段就多一次 Faiss 查询，对延迟有线性影响；实测 Top-3 已经足够覆盖绝大多数情况。

**RMS 而非其他能量度量**：`librosa.feature.rms` 一行调用、计算极快（128 维向量取均值），且 RMS 是工程上最朴素直接的能量代理；尝试过谱质心 / 谱平面度等更复杂指标，对最终 Top-1 准确率没有显著提升，不值得引入额外计算。

---

## 3. 模型训练过程与收敛情况

### 3.1 学习率与数据增强

模型初次训练时损失曲线长时间无法下降。后调整两个超参数：学习率从 `1e-4` 降至 `1e-5`，并在 Triplet 数据加载阶段引入幅值 0.01 的动态白噪声增强，损失开始稳定下降。`1e-4` 在 Triplet Loss 下经常出现"梯度爆炸式震荡"——单 batch 内 Loss 会从 0.5 跳到 8.0 再跌回 1.0，参数被反复推到不同盆地，无法收敛。`1e-5` 把每步更新幅度收敛到 1/10，配合 Adam 的自适应动量，损失曲线变得单调平稳。

### 3.2 损失收敛情况

在 `margin = 5.0` 的 Triplet Margin Loss 设定下，模型在第 3 个 epoch 收敛到平均 Loss 约 0.84。该数值表明：

- 大部分负样本与锚点的距离已达到 `5.0 - 0.84 ≈ 4.16`，超过 margin 阈值，符合 Triplet Loss 的训练目标；
- Loss 没有降到 0，说明模型并未对训练集过拟合，对白噪声扰动仍保留了泛化能力。

**为什么没有继续训练到更低 Loss**：在第 3 个 epoch 时已经观察到验证集（用同曲不同段的方式构造）上的检索 Top-1 命中率开始波动而不是单调上升，继续训练有过拟合风险。3 epoch 是经验上"刚好收敛、但尚未过拟合"的早停点。FMA-medium（25k 首）规模时，可能需要 5–8 epoch；FMA-large（10 万首）时反而 3–5 epoch 就够，因为数据多样性自带正则化。

### 3.3 训练加速：波形预解码缓存

训练过程中观察到 `librosa.load` 解码 MP3 是单 epoch 内最大的耗时瓶颈——8000 首歌每个 epoch 解码一遍要花数十分钟，远超 GPU 前向 / 反向的耗时。新增 `precompute_cache.py` 将 `data/raw/` 下所有音频一次性预解码为 NumPy 数组，存储在 `data/waveform_cache/`。后续训练直接 `np.load()` 读取，避免重复解码，迭代速度有明显提升。Dataset 类内部用 `os.path.isdir(cache_path)` 检测缓存是否可用，缓存未命中时透明回退到 `librosa.load`，新增歌曲时也不必强制清缓存。

---

## 4. 两个关键 Bug 的排查与修复

将训练好的模型接入实际检索流程后，遇到两个影响命中率的 Bug。两个 Bug 的修复过程是本项目工程量最大的部分之一。

### 4.1 Bug 1：预处理不对称导致的 Domain Shift

- **现象**：使用手机麦克风录音作为查询，返回的最佳命中汉明距离普遍在 48 bit 左右，无法识别。

- **诊断**：对比 `search.py` 与 `batch_extract.py` 的预处理代码，发现 `search.py` 中包含响度归一化（normalize）和静音切除（trim）操作，而建库时没有。这导致查询音频与库内指纹处于不同的特征域，构成 Domain Shift。

- **修复**：移除 `search.py` 中查询端单独的归一化与 trim 步骤，使两端预处理流水线严格一致。

### 4.2 Bug 2：哈希空间坍塌

- **现象**：使用与库中完全一致的数字原曲文件做查询，依然返回汉明距离 49 bit 的错误结果。

- **诊断**：打印模型每层输出后发现，Triplet Loss 在训练中将所有特征整体推向正半轴。当采用固定阈值 `(hash_out > 0)` 二值化时，128 维输出几乎全部大于 0，所有歌曲的指纹都坍塌成 `111…1` 这种同一个值，哈希空间退化到只剩一个点。

- **修复**：将固定阈值改为样本自身的中位数：

  ```python
  binary_code = (hash_out > hash_out.median())
  ```

  这种"动态中值"二值化强制每条 128 位哈希码中 0 和 1 各占 50%，从信息熵角度最大化了哈希编码的容量。修复后，原曲查询返回汉明距离 0 bit。

- **重要约束**：动态中值逻辑必须在 `search.py`（在线查询）与 `batch_extract.py`（离线建库）中严格保持一致；任何一方偏移都会再次引发命中率崩盘。该约束已写入项目 `AGENTS.md`，作为代码修改的红线。

---

## 5. 深度哈希领域的相关理论

针对"连续浮点特征转离散二进制码"过程中产生的量化误差，学界提出了多种缓解方法。本项目阅读并整理了如下方案，最终在工程稳定性考虑下采用了 Triplet Loss + 动态中值哈希组合，其余方案作为论文《未来工作》章节的延伸方向：

1. **ITQ（Iterative Quantization）**：通过 PCA 降维并迭代求解最优正交旋转矩阵，使特征在多维空间中尽量贴近超立方体顶点，减少符号化时的量化误差。

2. **Quantization Loss（量化惩罚损失）**：在训练损失中加入惩罚项 `L_quant = mean((|x| - 1)^2)`，约束网络输出趋近 ±1，从源头上减少二值化的截断误差。

3. **STE（Straight-Through Estimator）**：前向传播使用不可导的 `Sign()` 直接输出 0/1 哈希码，反向传播时用 `Tanh()` 的梯度替代，绕开 `Sign()` 不可导的问题，实现端到端训练。

本项目未实装上述方案的原因：当前业务目标（数字原曲检索）已被 Triplet + 动态中值组合完全满足；引入额外损失项或自定义反传逻辑会带来调参成本，性价比不高。在论文中作为"后续可探索方向"陈述更合适。

---

## 6. 全栈工程化实现

### 6.1 静态音频托管与试听

通过 FastAPI 的 `StaticFiles` 将本地 `data/raw/` 挂载到 `/audio` 虚拟路径。检索结果中包含可访问的 HTTP URL，前端用 HTML5 `<audio>` 标签即可试听。后端在拼接 URL 时统一处理 Windows 路径分隔符（`\` → `/`），避免跨平台问题。

### 6.2 用户注册与登录

数据库新增带 `UNIQUE` 约束的 `users` 表。在引入密码加密时遇到一个版本兼容问题：旧版 `passlib` 在初始化时会发送一段超长测试字符串，触发新版 `bcrypt` 的 72 字节长度保护，导致后端启动失败。最终决定不使用 `passlib`，直接调用 `bcrypt.hashpw` 与 `bcrypt.checkpw` 完成加盐哈希，问题解决。

### 6.3 收藏夹

后端新增 `favorites` 表（`user_id`, `file_path` 联合唯一约束），实现 `/favorite/toggle` 接口：传入歌曲路径，存在则删除、不存在则插入。

前端使用 ES6 `Set` 缓存已收藏路径，渲染结果列表时通过 `favSet.has(path)` 做 O(1) 判断，决定红心或空心图标。点击红心后本地立即更新 UI，不等待接口返回，响应感更顺。

### 6.4 JWT 鉴权

最初版本的实现存在一个水平越权漏洞：客户端在请求体中传入 `user_id` 字段，后端直接信任。这意味着任何登录用户修改请求体里的 `user_id` 就能读写他人的收藏夹。

修复方案：引入 PyJWT，登录成功后签发 HS256 Bearer Token（携带 `sub=user_id`、`username`、7 天 `exp`）。所有涉及用户数据的接口（`/favorite/toggle`、`/favorites/{uid}`、`/history`）都改为通过 `get_current_user(authorization)` 从 JWT payload 提取身份，不再读取请求体里的 `user_id`。

`JWT_SECRET` 通过环境变量 `JWT_SECRET` 注入；代码中保留的默认值仅供本地开发使用，在 `docker-compose.yml` 的 `environment:` 字段会被覆盖。

### 6.5 识别历史

新增 `search_history` 表，记录每次查询的源音频名、Top-1 命中结果、汉明距离、时间戳。前端通过 `GET /history` 拉取最近 50 条按时间倒序展示，`DELETE /history` 一键清空。

---

## 7. Docker 容器化与生产部署

### 7.1 镜像构建

`Dockerfile` 基于 `python:3.11-slim`：

- 通过 `apt-get install ffmpeg libsndfile1` 安装音频解码所需的系统库。
- 通过 `pip install --no-index --find-links=/tmp/wheels/` 从本地预下载的 wheels 安装 Python 依赖。

之所以采用离线 wheels 模式，是因为部署服务器（腾讯云）访问境外 PyPI 较慢，PyTorch CPU 版本（约 200 MB+）在线下载经常中断。本地通过 `pip download -r requirements.txt -d wheels/` 预先下载，再随构建上下文打入镜像，可以稳定完成构建。

`wheels/` 目录在 `.gitignore` 中屏蔽，避免约 337 MB 的二进制文件污染 Git 仓库。

### 7.2 反向代理与 HTTPS

`docker-compose.yml` 编排两个服务：

- `music-search`：FastAPI 容器，仅 `expose: 8000`，不对外开放端口。
- `caddy_proxy`：Caddy 容器，监听 80 / 443 对外服务。

`Caddyfile` 配置非常简洁：

```
evanesce.site, www.evanesce.site {
    reverse_proxy music-search:8000
}
```

Caddy 会自动通过 ACME 协议向 Let's Encrypt 申请并续期 SSL 证书，证书数据持久化在 `caddy_data` volume 中。从用户视角看，访问全程 HTTPS，整个证书申请过程零运维介入。

### 7.3 部署环境

| 项 | 配置 |
|---|---|
| 服务器 | 腾讯云轻量 ECS，Ubuntu 22.04，2 vCPU / 3.6 GB RAM / 59 GB SSD |
| 域名 | `evanesce.site`（主）、`www.evanesce.site`（CNAME） |
| 部署目录 | `/root/music-search-system/` |
| 数据持久化 volumes | `./data`、`./checkpoints`、`./music_hash.db`、`./music_hash.index` |

更新流程：

```bash
sudo -i
cd /root/music-search-system
git pull origin <branch>
docker compose up -d --build music-search
```

由于 Dockerfile 的层缓存命中，`pip install` 阶段大多数情况不会重新执行，单次构建大约 30 秒；旧容器优雅停机后新容器立即接管，用户感知到的服务中断在 3 秒以内。

### 7.4 部署中遇到的小问题

- **同名副本混淆**：服务器上一度存在 `/root/music-search-system/` 与 `/home/ubuntu/music-search-system/` 两份代码副本。通过 `docker inspect` 确认生产容器锁定 `/root/` 那份，后续清理掉冗余副本。
- **wheels 误删**：某次操作误删了 `wheels/` 目录，导致 `Dockerfile` 的 `COPY wheels/` 失败。已加入备份目录，必要时直接 `cp -r` 恢复。
- **`version: '3.8'` 弃用警告**：Docker Compose v2 不再识别顶层 `version` 字段，每次启动都会打印 WARN。删掉该字段即可消除，对功能无影响。

---

## 8. 前端实现要点

### 8.1 单文件 SPA

整站只有一个 `index.html`，通过 CDN 引入 Vue 3、WaveSurfer.js、Paper Shaders，**没有 npm / Vite / 构建步骤**。这种选择有两个考虑：

- 项目体量较小，引入构建工具链反而增加部署复杂度；
- 单文件 + CDN 模式让镜像体积最小（不需要打包前端产物），符合"轻量后端 + 轻量前端"的项目定位。

约束记录在 `AGENTS.md` 中：禁止后续引入 Vue CLI / Vite / webpack 等构建工具。

### 8.2 WaveSurfer.js 波形

上传文件后立刻渲染波形，可拖动定位试听。检索结果与底部的迷你播放器共用同一个 WaveSurfer 实例，避免重复加载。

### 8.3 Paper Shaders WebGPU 背景

使用 `Paper Shaders` 的 Solar Silk 1 着色器渲染品牌级动态背景。相比 CSS 渐变或 Canvas 2D，WebGPU 渲染在现代设备上是 60 FPS 且 CPU 占用极低。

考虑到 WebGPU shader 在后台标签页仍会占用 GPU，对笔记本续航和移动端发热不友好，封装了 `startShader` / `stopShader` 幂等闭包，并监听 `document.visibilitychange`：标签页隐藏时立即销毁 shader 释放资源，重新可见时再创建，恢复时间小于 100 毫秒。

### 8.4 持久化迷你播放器

底部的迷你播放器跟随路由切换持续存在，配合 `localStorage` 持久化当前曲目和播放进度。用户从识别结果跳到收藏页面时播放不会中断。

---

## 9. 测试覆盖与安全加固

### 9.1 单元测试

`tests/` 目录包含 4 个测试文件：

- `conftest.py`：搭建临时 SQLite + FastAPI TestClient 的 fixture，并通过环境变量注入 `JWT_SECRET`、`CORS_ORIGINS` 等配置。
- `test_auth.py`：覆盖注册、登录、用户名唯一性、密码长度边界（72 字节保护）等场景。
- `test_jwt.py`：覆盖 JWT 签发、过期、伪造、缺失 Header、非 Bearer 格式等失败路径。
- `test_preprocessing.py`：覆盖 `pad_audio`、`build_mel_spectrogram`、`binarize_hash`、`trim_silence`、`slice_best_windows` 等预处理函数，防止预处理对称性被无意打破。

运行方式：

```bash
pytest -v tests/
```

### 9.2 安全加固

- 所有用户数据接口都依赖 JWT，禁止从请求体读取 `user_id`；
- 密码使用 `bcrypt` 加盐哈希存储，绕开 `passlib` 的兼容问题；
- 敏感配置（`JWT_SECRET`、`CORS_ORIGINS`、`RATE_LIMIT_*`）全部通过环境变量注入，不进代码仓库；
- 注册和登录接口设置了基于 IP 的速率限制（默认 5 次 / 分钟），防止暴力破解。

---

## 10. 当前能力边界与下一步

### 10.1 已达成

- **数字纯净域**：在原曲文件检索上实现 Top-1 命中率 100%、汉明距离 0 bit；
- **加性白噪声场景**（SNR 10 dB）：Top-5 命中率 ≥ 92%；
- **检索性能**：8000 首 FMA-small 数据集，对应 20.32 万条子指纹，索引文件 3.2 MB，单次查询延迟 < 80 ms（CPU Faiss）；
- **完整产品形态**：用户注册 / 登录、检索、收藏、识别历史、试听、迷你播放器；
- **生产部署**：HTTPS 公网上线，已稳定运行于 `https://evanesce.site`。

### 10.2 未完全攻克

- **物理外录场景**：手机麦克风录音引入设备底噪、房间混响和频段扭曲，构成与训练数据不同的物理域偏移。当前模型在该场景下命中率明显下降。这属于深度学习里跨域检索的开放性问题，并非短期能完全解决。

### 10.3 下一步可做的事情

- **数据增强**：引入 SpecAugment、房间冲激响应（RIR）卷积模拟混响、设备 EQ 曲线模拟，提升模型在物理外录场景下的鲁棒性；
- **算法层探索**：尝试论文第 4 节列出的 ITQ、Quantization Loss、STE 等方案，对比它们与动态中值哈希在数字纯净域和加噪域上的表现差异；
- **数据规模**：将训练集从 FMA-small（8000 首）扩展到 FMA-medium（25000 首）或更大规模，验证哈希码长度是否需要相应增加；
- **检索流程**：探索分层检索（粗 → 精）或子指纹聚类，在更大规模库下控制延迟。

### 10.4 项目定位说明

本项目的定位是 **数字音乐版权查重与精准特征推荐系统**，而非通用的"听歌识曲"工具。在数字原曲域上，系统已经具备生产可用的检索精度和响应速度；在物理外录域上，受限于数据增强方法和算力预算，仍属于可继续研究的方向。这个边界在论文中也会明确陈述。
