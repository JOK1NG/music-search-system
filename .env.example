# 生产环境 JWT 密钥（必填，否则服务拒绝启动）
# 生成方式: openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-secret

# CORS 允许的来源（逗号分隔）
CORS_ORIGINS=http://localhost:8000,https://your-domain.com

# 开发环境可关闭 JWT 强校验（设为 1 则回退使用 dev 密钥）
# DEBUG=1
