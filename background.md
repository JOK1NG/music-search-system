# 项目背景总结：音乐音频相似度检索系统 / 海量数字音乐版权查重与相似度检索系统

## 项目概述

本项目旨在设计与实现一个端到端的“音乐音频相似度检索系统”，能够支持海量数字音乐的极速检索与推断。当前系统已能稳定处理纯净数字音频，并对白噪声增强场景具备一定鲁棒性；复杂手机外录、房间混响、设备频响差异等真实环境仍是后续需要重点评测和优化的域偏移问题。随着功能演进，项目从最初“能在实验室跑通的算法Demo”跃升为包含用户注册、登录鉴权、在线试听、搜索历史及个人收藏夹的商业 SaaS 级音乐产品原型。

## 核心技术架构

项目采用了现代化、轻量级的前后端分离架构，打通了“四核驱动”的数据链路：

- **AI 算法与信号处理层**: 使用 Python 3.11 环境。选用 `Librosa` 进行音频数字信号处理 (DSP) 提取 Mel 频谱图；选用 `PyTorch` 部署基于深度哈希网络 (DHN, Deep Hashing Network) 的 CNN 模型，将高维声学特征压缩为 128 位二进制指纹。
- **高速向量检索层**: 采用 Facebook 开源的 `Faiss` 引擎，基于 `IndexBinaryFlat` 算法实现针对成千上万二进制哈希码的毫秒级汉明距离 (Hamming Distance) 极速匹配。
- **全栈 Web 后端**: 采用 `FastAPI` + `Uvicorn` 作为高性能异步服务器接口，处理跨域与路由；选用 `SQLite` 作为关系型数据库提供歌曲元数据、收藏和搜索历史存储，并结合原生 `bcrypt` 与 `JWT` 提供多用户状态管理能力。
- **前端交互层**: 使用 `Vue 3` (Composition API) 结合 `Axios` 进行页面构建与异步请求，引入 `WaveSurfer.js` 根据音频数据渲染动态可视化波形。

## 核心算法难点与破局 (科研与工程亮点)

本项目完整经历了深度度量学习 (Deep Metric Learning) 的典型科研踩坑与优化过程：

1. **时间平移敏感性与滑动窗口**: 针对 CNN 在音频时间偏移（如截取片段不佳）下特征突变的软肋，设计了“滑动窗口选秀法”，通过提取音频片段中绝对声学能量 (RMS) 最大、最饱满的片段对抗时间对齐误差。
2. **哈希空间坍塌与动态中值哈希**: 在使用 Triplet Loss 优化距离时曾出现所有输出坍塌为一模一样的同质化哈希码的情况。通过引入“自适应动态中值哈希”替代传统的死板 0 值二值化阈值，最大化了哈希码的信息熵，从根本上恢复了哈希指纹的区分度。
3. **领域鸿沟 (Domain Gap) 与抗噪鲁棒性**: 开发中详细论证了干净数据集与真实外录场景（设备底噪、频段丢失、房间混响）的分布差异。当前实现已包含白噪声增强训练、短音频补齐、滑动窗口和 RMS 能量窗口选择等机制，用于提升时间偏移和简单噪声场景下的命中率；端点检测去静音 (VAD)、响度归一化、混响/设备响应增强与 SpecAugment 仍属于后续可验证的优化方向。
4. **损失函数的深层次理论探索**: 当前训练代码采用 `margin=5.0` 的 Triplet Margin Loss（相似度损失推开异类），并结合动态中值阈值完成哈希二值化；Quantization Loss（量化评估损失）与 STE（直通估计器）等方案属于后续优化探索方向，可作为论文《系统优化探索》及展望章节的理论储备。
